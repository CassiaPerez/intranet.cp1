# Backend Configuration
PORT=3006
HOST=127.0.0.1
NODE_ENV=development
WEB_URL=https://intranet.grupocropfield.com.br
JWT_SECRET=cropfield-secret-key-2025

# Google OAuth Configuration (opcional)
# Para configurar: npm run setup-oauth
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=https://intranet.cropfield.com.br/auth/google/callback

# Database
# SQLite database will be created automatically in ./data/database.sqlite
DATABASE_PATH=/var/www/intranet/data/database.sqlite

# Frontend Configuration (for Vite)
# VITE_API_URL=
# Leave empty for development - requests will go through Vite proxy to localhost:3006
# Only set this for production builds

# Production Configuration (when deploying)